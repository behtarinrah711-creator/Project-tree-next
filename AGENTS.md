# Saosa Agent Rules

- فقط درخواست فعلی، وضعیت مخزن و این فایل ملاک است.
- سابقه گفتگو و Personal Context را بررسی نکن، مگر اینکه کاربر صریحاً به کار یا تصمیم قبلی اشاره کند.
- فقط فایل‌ها و بخش‌های مستقیماً مرتبط را بررسی کن؛ از جست‌وجوی سراسری، بررسی کل معماری، تاریخچه Git و خروجی‌های طولانی خودداری کن.
- به‌محض پیدا شدن اطلاعات کافی، بررسی را متوقف و کوچک‌ترین تغییر صحیح را اجرا کن.
- کد نامرتبط را Refactor نکن و از Hack، Timeout و پیاده‌سازی تکراری استفاده نکن.
- از Live، مرورگر و Computer Use برای کد، GitHub، CI، Merge یا Deploy استفاده نکن.
- برای عملیات Remote فقط از GitHub Connector استفاده کن؛ اگر در دسترس نبود، متوقف شو و مانع دقیق را اعلام کن.

## Testing

- تست باید متناسب با تغییر باشد.
- برای رنگ، فاصله، متن، آیکون، CSS selector یا شرط کوچک، فقط تست مستقیم مرتبط یا بررسی حداقلی انجام بده؛ کل Unit/E2E ممنوع است.
- تست کامل فقط با درخواست صریح کاربر یا تغییر در معماری مشترک، Route، Auth، Sync، Persistence یا Schema مجاز است.
- هیچ تستی را حذف، غیرفعال یا ضعیف نکن.

## GitHub Flow

1. از آخرین نسخه Branch هدف، یک Branch مستقل بساز.
2. فقط فایل‌های ضروری را تغییر بده و Diff همان فایل‌ها را بررسی کن.
3. تست متناسب را اجرا کن.
4. Commit واضح بساز و Branch را Push کن.
5. با GitHub Connector یک PR به `main` بساز؛ PR موجود را تکرار نکن.
6. شماره و لینک PR را کوتاه گزارش کن و منتظر CI نمان، مگر اینکه کاربر بخواهد.

## Check, Fix and Merge

- با دستور «چک کن و مرج کن»، فقط PR و CI مربوط به آخرین Head SHA همان PR را بررسی کن.
- اگر CI در حال اجراست، با فاصله منطقی بررسی کن و Poll مکرر انجام نده.
- اگر CI شکست خورد، فقط Job، Step و Log مرتبط را بررسی و علت واقعی را مشخص کن.
- اگر رفع خطا در محدوده درخواست است، اصلاح را روی همان Branch Commit و Push کن؛ تست را برای سبزشدن ضعیف نکن.
- فقط وقتی آخرین CI لازم سبز، PR قابل Merge و بدون Conflict است، با Expected Head SHA آن را Merge کن.
- Merge را Deploy تلقی نکن؛ Deploy را فقط با درخواست کاربر بررسی کن.

## Communication

- توضیحات، خروجی ابزارها و گزارش نهایی را کوتاه نگه دار.
- فقط تغییر انجام‌شده، تست مرتبط، شماره PR، علت خطا، Merge و Deploy درخواستی را گزارش کن.
